# Keep repository text files normalized to LF across platforms.
* text=auto eol=lf

# Windows-native script types can keep CRLF when they are added later.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
